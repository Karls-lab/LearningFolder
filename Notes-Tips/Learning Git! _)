Learning Git! :)

Simple order of commands:
1. Get and rsa ssh key and upload it to github
2. clone the repository using ssh
3. add your files with git add .
4. commit your files with git push origin: git commit -m "Exammple message"
5. Use git pull to update local machine with files on github
5. git stash to save temp files in order to pull


There's git guis that can help visualize all the git visuals. 
Im trying out git lens by krakken.

Git Auth:
$ ssh-keygen -t ed25519 -C "your_email@example.com"
ssh-key: blackSphinx53
my ssh key is hidden in x1b3dead cd  /.ssh
My public key in id_rsa.pub is what I give to github.
git config --global user.name "FIRST_NAME LAST_NAME"
git config --global user.email "MY_NAME@example.com"


Emily's suggestions:
    git checkout -b feature /branch-name
    ~
    git add
    git commit -m "what have I done"
        go into github, requeset a pull request
    git push -u origin feature/branchname
