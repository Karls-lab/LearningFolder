
View your gpu statistics in real time
    watch -n 1 nvidia-smi

install Tensorflow:
    python3 -m pip install tensorflow[and-cuda]

install CUDA toolkit 
    sudo apt-get install nvidia-cuda-toolkit

install cuDNN
    sudo apt-get install libcudnn8
